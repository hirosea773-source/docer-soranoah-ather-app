# Cline Rules

- 回答・説明は日本語で行う。
- 既存ファイルを確認してから変更する。
- 推測でファイル構成を決めない。
- 一度に大規模変更しない。
- 変更前に対象ファイルと変更理由を短く説明する。
- テスト失敗時は、原因候補、確認コマンド、修正方針を分けて説明する。
- セキュリティ情報、APIキー、.env の値を出力しない。
- package.json、Dockerfile、devcontainer.json、playwright.config.ts、supabase関連設定を変更する場合は影響範囲を説明する。
- 勝手にgit commitしない。
